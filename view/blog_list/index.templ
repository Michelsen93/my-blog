package blog_list

import (
    "github.com/Michelsen93/my-blog/model"
)

templ Show(postsMetaData []model.BlogMetaData) {
    <ul>
        for _, item := range postsMetaData {
            <li><a href={ templ.SafeURL("blog" + item.Path) }>{item.Title}</a></li>
        }
    </ul>
}
